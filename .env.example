# Copy to `.env.local` (gitignored, never commit it):
#     cp .env.example .env.local

# Who is running this capture, shown in the User-Agent so a publisher can
# reach you instead of silently blocking. A GitHub identity and repo URL
# identify you with no personal data. Every fork must set its own.
WSS_CONTACT=neldivad +https://github.com/neldivad/wss-cloud-footprint
